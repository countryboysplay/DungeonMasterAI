name: Apply r42 readied UI

on:
  push:
    branches: [feature/r42-readied-player-rolls]
    paths:
      - '.github/workflows/apply-r42-readied-ui.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/r42-readied-player-rolls
          fetch-depth: 0
      - name: Apply readied reaction integration codemod
        run: python3 .github/scripts/apply_r42_readied_ui.py
      - name: Commit readied reaction routing
        run: |
          git config user.name countryboysplay
          git config user.email 31964073+countryboysplay@users.noreply.github.com
          git add windows/src/DungeonMasterAI.App/MainViewModel.cs windows/src/DungeonMasterAI.App/MainViewModel.PlayerRolls.cs windows/src/DungeonMasterAI.Engine/GameEngine.PlayerDecisions.cs windows/src/DungeonMasterAI.Engine/DmToolRouter.cs
          git diff --cached --check
          git commit -m "r42: route readied reactions through player choice and rolls"
          git push origin HEAD:feature/r42-readied-player-rolls
